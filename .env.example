# Shared service endpoints for local development documentation.
# Copy values into each project's own environment file as needed.
SUPABASE_URL=
SUPABASE_ANON_KEY=
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sonman
FCM_PROJECT_ID=
